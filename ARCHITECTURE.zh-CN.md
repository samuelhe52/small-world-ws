# 分布式架构

## 进程与存储模型

`serve` 进程负责三项工作：HTTP API 和静态前端、实验协调，以及进度聚合。它
会使用隐藏的 `worker --id i` 命令启动 `W` 个相同可执行文件的实例。

对于 `N` 个节点，worker `i` 负责以下半开区间：

```text
[floor(N * i / W), floor(N * (i + 1) / W))
```

每个 worker 只为自己负责的节点存储 `Vec<Vec<u32>>` 邻接表。邻居 ID 保持全局
编号。协调器不会接收邻接表。worker 的请求和响应使用 `bincode` 序列化，并通过
worker 的标准输入/输出管道，以带长度前缀的帧进行传输。

这里采用的是本机多进程分布：存储确实分片在相互隔离的地址空间中，但传输使用
本地 IPC，而不是多台物理主机。将管道替换为 TCP 不会改变下文所述的所有权关系
或算法协议。

## 分布式 WS 构建与重连

初始环形网络的构建不需要通信。每个 worker 都可以根据 `N` 和 `K`，推导自己
负责节点的 `K/2` 个左邻居和右邻居。

重连使用标准的顺时针规范边 `(u, u+d)`，其中 `d=1..K/2`。各分片轮流作为权威
重连分片：

1. 当前 worker 处理固定端点 `u` 属于自己的所有规范边。
2. 它在 `u` 的完整本地邻接表中检查重复边。
3. 它立即更新 `u`。
4. 如果旧的或新的另一端点属于远端，就返回一条以该端点所属 worker 为目标的
   删除或添加变更。
5. 协调器路由这些变更，并等待确认，然后才激活下一个分片。

串行的分片权威机制是有意设计的。它无需分布式锁或回滚，就能让重复检查和边的
对称性保持确定性。worker 仍然独立完成图存储和端点变更。重连完成后，协调器只
请求各分片的邻接项数量，并验证全局不变量 `sum(degrees) = N*K`。

## 分布式聚类系数

当 `N` 较大时，演示项目会均匀采样最多 20,000 个顶点，且不放回。对于每个采样
顶点 `u`，其所属 worker 会枚举所有无序邻居对 `(a,b)`。检查 `(a,b)` 是否存在
时，请求会被路由到 `owner(a)`，因为只有该进程存储 `adj[a]`。

目标 worker 按采样顶点索引聚合成功的查询。协调器合并计数，并计算：

```text
C_u = 2 * neighbor_edges / (degree(u) * (degree(u) - 1))
```

之后对样本求平均。UI 会将 `C` 标记为采样值，而不会将其展示为所有顶点的精确
结果。

## 按层同步的分布式 BFS

每个采样源点都会在所有 worker 上执行一次协同 BFS：

```text
开始处理源点
    ↓
所有 worker 扩展本地 frontier
    ↓
按目标所属 worker 对远端发现结果分组
    ↓
路由、去重，并接收尚未访问的顶点
    ↓
检查全局 frontier 和终止条件
    ↺ 下一层
```

每个 worker 都维护自己的本地 visited 位图、当前 frontier 和下一 frontier。它
不会读取其他 worker 的邻接表。协调器会汇总第 `d` 层接收的发现数量，并累加距离
贡献 `d * count`。当所有 worker 都报告下一 frontier 为空时，该源点处理完成。

源点采用不放回的均匀采样。由于每次 BFS 都使用完整的 worker 集群，所以源点会
按顺序执行；但每一层中代价较高的扩展会在所有 worker 进程中并发进行。

## 仪表盘与进度

Axum 服务器提供：

- `GET /api/status`：返回当前阶段、frontier 大小、结果以及内存中的运行历史。
- `POST /api/run`：验证配置并启动一次实验。

React 前端每 400 ms 轮询一次状态。运行期间，控件会被禁用；worker 数量会反映
当前运行中的子进程数量；阶段进度会持续更新；每个 worker 的 frontier 大小会
驱动活动状态栏。运行历史保存在服务器内存中，服务器重启后会重置。

## 参考运行

在 Apple M1 Pro 开发主机上进行过一次 release 模式运行，使用 4 个 worker、
`N=1,000,000`、`K=10`、`p=0.05`、32 个 BFS 源点和 20,000 个聚类样本：

| 结果 | 数值 |
| --- | ---: |
| 邻接项 | 10,000,000 |
| 重连边 | 250,848 |
| 跨分片协议项 | 11,501,939 |
| BFS 访问顶点数 | 32,000,000 |
| 平均路径长度 `L` | 11.758442 |
| 采样聚类系数 `C` | 0.573552 |
| 端到端耗时 | 2.038 s |

该耗时仅作本地参考，不代表可移植的性能保证。

## 有意保留的限制

- worker 在同一台主机上运行并使用管道，因此没有网络故障模型。
- 协调器是单一的路由和同步节点。
- 运行历史不会持久化。
- 节点数超过 20,000 时，聚类计算使用采样。
- 重连分片一次只允许一个分片作为权威分片；端点存储是分布式的，但重连决策
  阶段不是并发提交的。

这些边界让演示项目保持易于理解，同时保留早期 Rayon 实现所缺少的关键属性：
隔离的分片存储、明确的跨分片消息，以及协同式分布式 BFS。
